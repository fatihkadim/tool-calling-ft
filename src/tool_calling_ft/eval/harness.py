"""Eval harness: bir checkpoint'i yukleyip test seti uzerinde metrics.py'daki
fonksiyonlari calistirir, sonucu reports/ altina JSON olarak yazar.

TODO: load_model(checkpoint_path, method), run_inference(model, test_set),
      run_eval(checkpoint_path, method) -> dict
"""
